version: '3'
services:
  web:
    build: 
      context: .
      dockerfile: docker/dev/Dockerfile
    ports:
      - '3000:3000'
    networks: 
      - smart-network
    volumes:
      - .:/home/app/smartadmin
      - /home/app/smartadmin/node_modules